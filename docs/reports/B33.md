# B33 — Web viewer: on-demand run buttons + an editable in-place translation view

**Status: BLOCKED, stopped per the card's stop rule.** Only the unblocked pure-helper piece was
completed; `RunButton.svelte` / `run.ts` / `EditView.svelte` / `App.svelte` wiring / the six view
edits are **not started** — the card's premise is false in this worktree (see Questions).

## Changes
- `webui/src/edit.ts` (new): `textByRegion` and `overlayStyle` exactly per the card's interface
  (reuses `alignLabel` from `./layout`; includes `white-space: pre-wrap` and `sans-serif`, see
  Deviations).
- `webui/src/edit.test.ts` (new): 6 tests covering acceptance tests 3 and 4 from the card
  (empty/two-lines/duplicate for `textByRegion`; substring assertions for `overlayStyle`, plus a
  center-default/no-font-filename test).
- Nothing else. No existing file was modified.

## Tests
- `cd webui && npm run test` → **76 passed (8 files)**, including the 6 new `edit.test.ts` tests.
- `cd webui && npm run check` → **0 errors, 0 warnings (132 files)**.
- `npm install` was run in `webui/` first: the fresh worktree had no `node_modules` (62 packages,
  0 vulnerabilities). Python-side checks were not run — no Python was touched.

## Deviations
- `overlayStyle` adds `white-space: pre-wrap;` (so a multi-line final text reads with its line
  breaks in the overlay) and `font-family: sans-serif;` (the card's definition says "a generic
  sans-serif at `item.size_px` is enough" — making that explicit in the style). Both are additive;
  the acceptance-test substrings are unaffected.
- Nothing else deviates; the stopped work means most of the card is simply absent, not changed.

## Questions (the blocker)
1. **The worktree is 2 commits behind `main`, so the card's backend premise is false here.**
   The card says `webui/src/api.ts` "already has" `postRun`/`getJob`/`putFinalLine` (+ `Job`,
   `RunResult`, `JobStatus`) and that the `POST …/run` / `GET /api/jobs/{id}` /
   `PUT …/final/{region_id}` endpoints are already built ("director work, this session"). None of
   that exists in this worktree:
   - `B33` points at `74113d4` (same as `origin/main`), while local `main` is at `f97280d`
     ("web: on-demand pipeline run + editable final-line endpoint", 2026-09-23 14:35, +156 lines in
     `src/omniscan/web/app.py`, +50 in `webui/src/api.ts`, +7 in `cli.py`, tests) with `7ef9900`
     (Reader/Layout scroll sync) below it — exactly `[ahead 2]`.
   - Verified: `git show f97280d:webui/src/api.ts` contains the three wrappers (signatures match
     the card's contract verbatim — no contract drift); this worktree's `app.py` route table has
     only GET endpoints + the filter-restore POST, and no `jobs` module exists under
     `src/omniscan/`.
   - This worktree was evidently created (or its branch cut) before those commits landed.
   **I could not fix this myself:** `.builder/settings.json` deny-lists `git merge`, `git rebase`
   and `git reset --hard` for builders (per policy, merging is the director's job), and hand-copying
   `src/omniscan/**` / `api.ts` out of the main checkout would violate the card's file allowlist
   ("anything not listed is off-limits — especially `src/omniscan/**`"; api.ts is out of scope). So
   I stopped rather than guess, per the card's stop rule.
   **Suggested fix (director, from this worktree):** `git rebase main` (my WIP commit re-applies
   cleanly on top; `git merge --ff-only main` only works if this WIP commit is first dropped or the
   branch is reset). Then resume the card — everything else should then hold:
   - I verified the card's six replace-target `<p>` lines are byte-identical on `f97280d`'s
     versions of the views (LayoutView line 63, ReaderView line 110 checked directly; the other
     four are untouched by the two commits), so the view edits apply unchanged.
   - `7ef9900` touches `ReaderView.svelte` and `LayoutView.svelte` — files this card also modifies;
     after the update, the new (post-sync) versions are the correct base, not the copies in this
     stale worktree.
2. (Minor, for whoever verifies) `npm run test`/`npm run check` need `node_modules` in the
   worktree's `webui/`; I ran `npm install` here, so a resume in this worktree does not need it
   again. If the worktree is recreated instead, re-run `npm install` there first.