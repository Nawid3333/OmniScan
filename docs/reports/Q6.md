# Q6 — Mutation review of the slicer strategies

## Summary

- **33 mutants** written by hand across `src/omniscan/slicer/strategies.py` (27) and `src/omniscan/slicer/compare.py` (6), list at `tests/mutants/slicer_strategies/strategies.py`.
- **First pass (existing tests only):** 23 killed, **10 survived**, 0 invalid, 0 equivalent.
- **All 10 survivors are real gaps** (none equivalent); each is now killed by a dedicated gap test — proven with
  `mutate.py run … --only 2,6,11-14,17,20,22,25 -t <gap tests>` → `10 killed, 0 survived`.
- **Gap tests:** 11 new tests in `tests/unit/test_slicer_strategies_mutation_gaps.py` (10 of them kill a former
  survivor; 2 of those also pin behaviour for mutants the existing suite already killed) + 1 new test appended to
  `test_slicer_compare.py` (kills the "median loses int()" mutant).
- Final tally after gap tests: **33/33 killed.**

Process note: a first mutation run was contaminated — it started before a newly appended test landed in
`test_slicer_compare.py`, and that test briefly failed on the original code (wrong expected median, fixed), so
mutants 11+ were spuriously "killed" by it. The whole suite was re-run against a green baseline; the table below
reflects the clean run.

## Mutant table

`(before)` = verdict from the three existing test files alone; `killer` = the test that fails under the mutant.

| # | module:line | mutation | before | killer |
|---|---|---|---|---|
| 0 | strategies.py:30 | explicit `strategy=` argument ignored (`else strategy` → `else cfg.strategy`) | killed | `test_dispatcher_follows_cfg_and_explicit_strategy` |
| 1 | strategies.py:31 | invert unknown-name check (`not in` → `in`) | killed | `test_dispatcher_unknown_name_message` |
| 2 | strategies.py:49 | drop empty-strip page guard (`and height > 0`) | **survived** | `test_page_empty_strip_with_no_files_has_no_fallback` (new) |
| 3 | strategies.py:49 | page fallback `and` → `or` | killed | `test_page_cuts_at_file_boundaries` |
| 4 | strategies.py:65 | fixed loop bound `>` → `>=` | killed | `test_fixed_examples` |
| 5 | strategies.py:68 | fixed tail guard `height > pos` → `>=` (0-row strip gets an empty slice) | killed | `test_fixed_examples` (H=0 case) |
| 6 | strategies.py:70 | tail-merge threshold `<` → `<=` | **survived** | `test_fixed_tail_exactly_min_height_is_not_merged` (new) |
| 7 | strategies.py:71 | merge reads `boundaries[-1]` (the tail) instead of `[-2]` | killed | `test_fixed_merges_short_tail_into_previous` |
| 8 | strategies.py:69 | fixed tail slice `forced_cut` False → True | killed | `test_fixed_examples` |
| 9 | strategies.py:72 | merged slice drops its `forced` flag | killed | `test_fixed_merges_short_tail_into_previous` |
| 10 | strategies.py:66 | fixed cut `forced_cut` True → False | killed | `test_fixed_examples` |
| 11 | strategies.py:79 | gutter variance `<` → `<=` (boundary rows become gutters) | **survived** | `test_gutter_variance_threshold_is_strict` (new) |
| 12 | strategies.py:90 | gutter run centre floor → ceil | **survived** | `test_gutter_run_centre_is_floored` (new) |
| 13 | strategies.py:90 | gutter run length filter `>=` → `>` | **survived** | `test_gutter_min_rows_boundary` (new) |
| 14 | strategies.py:90 | `gutter_min_rows` threshold − 1 | **survived** | `test_gutter_min_rows_boundary` (new) |
| 15 | strategies.py:80 | drop the leading zero pad (edge-touching gutter runs lost / zip raises) | killed | `test_simple_gutter_golden` (odd-length runs shift every centre) |
| 16 | strategies.py:101 | simple_gutter loop `>` → `>=` | killed | `test_simple_gutter_handles_60k_rows` (remainder is exactly `max_height`) |
| 17 | strategies.py:103 | candidate window top `<= hi` → `< hi` | **survived** | `test_simple_gutter_cut_exactly_at_window_max_is_used` (new) |
| 18 | strategies.py:105 | candidate pick `min` → `max` | killed | `test_simple_gutter_golden` |
| 19 | strategies.py:105 | tie-break `(abs(c-target), c)` → `(…, -c)` | killed | `test_simple_gutter_tie_prefers_smaller_cut_row` |
| 20 | strategies.py:105 | key `abs(c - target)` → `abs(c - pos)` | **survived** | `test_simple_gutter_picks_the_cut_closest_to_target` (new) |
| 21 | strategies.py:109 | forced fallback cut loses its flag (`True` → `False`) | killed | `test_simple_gutter_no_gutter_forces_target_cuts` |
| 22 | strategies.py:130 | blank flag `all` → `any` | **survived** | `test_slice_with_one_uneven_row_is_not_blank` (new) |
| 23 | strategies.py:132 | file-overlap test `and` → `or` | killed | `test_fixed_source_files_mapping` |
| 24 | strategies.py:132 | file start `<` → `<=` | killed | `test_page_cuts_at_file_boundaries` (file starting exactly at a slice's `y1`) |
| 25 | strategies.py:139 | `strip_width` / `strip_height` dims swapped | **survived** | `test_finalize_records_the_strip_dimensions` (new) |
| 26 | strategies.py:143 | params drop `"strategy": name` | killed | `test_every_strategy_tiles_the_strip` |
| 27 | compare.py:46 | summary `min(heights)` → `max(heights)` | killed | `test_summarize_counts_and_heights` |
| 28 | compare.py:48 | summary `max(heights)` → `min(heights)` | killed | `test_summarize_counts_and_heights` |
| 29 | compare.py:47 | summary median → mean | killed | `test_summarize_median_even_count` |
| 30 | compare.py:47 | `median_height` loses its `int()` (250.0 instead of 250 in JSON) | **survived** | `test_summarize_median_height_is_an_int` (new, in `test_slicer_compare.py`) |
| 31 | compare.py:49 | `forced` counts blank slices | killed | `test_summarize_counts_and_heights` |
| 32 | compare.py:51 | `cuts` include the last slice's `y1` | killed | `test_summarize_counts_and_heights` |

## New tests

`tests/unit/test_slicer_strategies_mutation_gaps.py` (11 tests):

1. `test_page_empty_strip_with_no_files_has_no_fallback` — kills #2: a 0-row strip must take the page path (empty artifact, no `"fallback"` param), not fall back to smart.
2. `test_fixed_tail_exactly_min_height_is_not_merged` — kills #6: a tail of exactly `min_height` rows stays its own slice (H=7500 → 3 slices).
3. `test_gutter_variance_threshold_is_strict` — kills #11: rows with variance exactly at `gutter_variance` are not gutters (100/104 rows at threshold 4.0; float-exactness verified: `torch.var` yields exactly 4.0).
4. `test_gutter_run_centre_is_floored` — kills #12: an odd-length run (9 rows) cuts at `(s+e)//2`, not ceil.
5. `test_gutter_min_rows_boundary` — kills #13 and #14: a 7-row run is below the 8-row threshold, an 8-row run is exactly at it.
6. `test_simple_gutter_strip_exactly_max_height_is_one_slice` — H == `max_height` is one uncut slice (pin; mutant #16 was already killed by the 60k test).
7. `test_simple_gutter_cut_exactly_at_window_max_is_used` — kills #17: a gutter centred exactly on the window's `hi` is a candidate.
8. `test_simple_gutter_picks_the_cut_closest_to_target` — kills #20: with candidates at `pos+1500` and `pos+4000` (target `pos+3000`), the cut closest to *target* wins, not to `pos`.
9. `test_slice_with_one_uneven_row_is_not_blank` — kills #22: one non-uniform row (alternating 0/255) makes a slice non-blank even if 2999 rows are uniform.
10. `test_source_file_starting_exactly_at_slice_start_is_excluded` — a file whose `y0` equals a slice's `y1` maps only to later slices (pin; mutant #24 was already killed by the page test).
11. `test_finalize_records_the_strip_dimensions` — kills #25: `strip_width`/`strip_height` must reflect the strip (W=720, H=2000).

Appended to `tests/unit/test_slicer_compare.py`: `test_summarize_median_height_is_an_int` — kills #30 (median of an even count is a float; the summary keeps it an int).

## Equivalent mutants

None among the 33. (Considered and rejected as equivalent before writing: `strict=True` → `strict=False` in the
`zip(starts, ends)` of `gutter_cut_rows` — the +1/−1 change array always produces matched counts; dropping the `c`
tie-break in `min(candidates, key=…)` — `cut_rows` is ascending, so `min` already picks the smallest on a tie;
`(y0, height, forced)` → `(y0, height, True)` in the fixed-tail merge — `boundaries[-2]` is always a forced cut
there.)

## Questions

None — no suspected production bugs; every gap test passes on the original code.