# Checkpoint — 2026-09-23, updated 2026-09-24 (Windows native, `V:\OmniScan`)

## 2026-09-24 session (director, no card)
1. **Scope decision: dropped cross-platform/multi-GPU portability entirely**, owner's explicit instruction.
   `pyproject.toml`'s `cpu`/`cuda`/`mps` extras, `[tool.uv.conflicts]` and their index/source entries are
   removed — `rocm-gfx1201` is now the project's only backend (`uv.lock` regenerated, `uv sync --extra
   rocm-gfx1201 --extra gui` verified clean, full `pytest -m "not gpu"` green). `HANDOFF.md`/`PLAN.md`/
   `README.md`/`CLAUDE.md` updated to match. `.github/workflows/ci.yml` was rewritten the same day (F15,
   `66046868`) to a single `windows-latest` job syncing `rocm-gfx1201 + gui`, no OS/GPU matrix. **Resolved
   2026-09-25: pushed and verified green** — `uv sync --extra rocm-gfx1201 --extra gui` installs cleanly on
   a GitHub-hosted runner with no AMD GPU (answers F15's open question), and `main`'s CI has been green
   since (the one red run in between, `66046868`'s own first push, failed on an unrelated pre-existing
   `test_model_watch_workflow` regression from an earlier commit, since fixed). `runner-images.yml` was
   added after this to test `ci.yml` against upcoming `windows-latest` images. See `docs/OPEN_QUESTIONS.md`
   (F15, resolved).
2. **OCR per-language defaults added** (`core/config.py::_apply_lang_ocr_defaults`): previously there was no
   language-aware default at all — every series silently got Korean PP-OCRv5 models unless its own
   `series.toml` hand-set `engine`/`rec_model`. Now a series.toml (or the repo default) that sets `ocr.lang`
   without also naming an engine gets the qualification suite's measured-best engine automatically: ko/zh →
   `paddleocr_vl`, ja → `ppocr-v6-medium`. An explicit engine/det_model/rec_model always still wins. Tests
   added in `tests/unit/test_core_series_config.py`; ruff/pyright/pytest all clean.
3. **F2c (fixed-position watermark wiring) confirmed already done** — `docs/CHECKPOINT.md`'s "Not done" list
   was stale; `detect/stage.py` has called `resolve_watermark_regions`/`reclassify_watermark_position_regions`
   since `6835760` (2026-09-23). Removed from "Not done" below.

**New session? Read `docs/HANDOFF.md` first** (reading order, working agreement with the owner, lessons), then this file, then `docs/NEXT.md` (the ordered work queue with builder cards).
`docs/PLAN.md` is the master plan (see its "Plan revisions" section); this file is the current-state snapshot. Repo: **`V:\OmniScan`**, remote `Nawid3333/OmniScan` on GitHub (`main` is pushed after every merge).
Local data lives in `V:\OmniScan\data\` (gitignored; see its `README.txt`): `raws/` (chapters to translate, = `paths.library_root`), `translated-check/` (ground truth with the same Series/Chapter layout), `output/` (finished pages), `work/`+`work_qual/` (pipeline artifacts, safe to delete); machine config in `C:\Users\limex\.config\omniscan\config.toml` (paths only; the GPU is chosen by `gpu.device = "auto"`).

**Session end state (2026-09-23, later): main is clean, no worktrees, no builder slots held except one in flight (B32), nothing else running.** Safe to stop here. ~3 900 tests, ruff and pyright clean.

## State in one paragraph
**This was a long session in two halves.** First half: found and fixed three real performance/correctness bugs in the OCR/translation path (see below), then changed the translation default on the owner's instruction. Second half (owner: "build the rest so we can be done"): did the real A1 tuning pass — ran the actual Solo Leveling chapters end to end, looked at the rendered pages, and found **four more real, previously-invisible bugs**, all now fixed and verified against the real output:
1. **LaMa silently skipped over-wide/tall regions** (`inpaint/lama_pipeline.py`, director fix, no card — hardware-verified GPU tensor code): a fixed 512px window couldn't hold a 513px-wide full-width narration caption, so it was skipped entirely, leaving the original Korean fully visible under the English translation. Now tiles such a region across multiple windows and stitches the result — no region is skipped any more.
2. **Cross-class duplicate detection** (card **D1**, merged): a `text_bubble` and a `text_free` detection for the same caption (IoU 0.55, just under the 0.6 dedup threshold, because one box was a tight subset of the much larger other) both got translated and typeset, producing overlapping garbled text. Fixed by also checking containment (IoA ≥ 0.85) alongside IoU.
3. **Oversized bubble pairing** (card **D2**, merged): a text box paired with a "bubble" detection 11x larger than its own text (a detector artifact spanning almost a whole panel), so the typesetter placed the caption using the wrong box's position. Fixed by rejecting a bubble candidate more than 8x the text's own area (a threshold picked from a clean gap in 225 real bubble/text area ratios across 6 chapters).
4. **Site-injected ad/watermark text translated as if real dialogue** (card **F2b**, merged): the raw source (wfwf504) injects a gambling-ad watermark ("구글검색"/"라이브스코어"/"가상토토"/"스포위키") that the detector sometimes catches as an ordinary region — on Solo Leveling ch2 alone, 17 of 108 non-watermark-kind regions were this ad, rendered in English as if part of the official release. Now reclassified to `kind="watermark"` post-OCR by a text-pattern match (`config/watermark_text.toml`), which every downstream stage already excludes from translation (per the owner's own E4 decision: watermarks stay on the page, just untranslated).

Also this half: **B11b** (render-stage web views — inpaint raw↔clean slider, layout overlay) merged and verified against real data (hit the new endpoints directly, got back real bytes/a real patch PNG); **B32** (doctor's required-Ollama-models check derived from the real profile/judge config instead of a hardcoded, now-stale list) in flight; the desktop app's **Import page wired into the sidebar** (was built, never connected — now a sixth page, verified with real screenshots).

**First half of the session** (context for the fixes above): (1) the OCR qualification suite (O1c, merged) was rebuilding its VRAM manager per chapter instead of per candidate, and a `series.toml` was silently clobbering the candidate's OCR config at two separate merge points — both fixed directly by the director (`core/stage.py`, `eval/qualify.py`), since one crossed into `core/**`. (2) PaddleOCR-VL's shipped `generation_config.json` had `use_cache: false` inherited from training, so every generation step recomputed the whole sequence; forcing `use_cache=True` (and *not* batching — measured 9-32x slower batched, reversing an earlier assumption) took real chapters from 749-864s to 56-100s at identical quality (`docs/benchmarks/ocr-qualification-results.md`). A side effect: long text blocks silently truncated at `vl_max_new_tokens` were being scored as fully trustworthy — now scored 0.0 so `ocr.drop_conf` filters them. (3) Ollama's own context-length setting (256K on this machine) was pushing 11 of translategemma:12b's 49 layers onto the CPU, and two local models were staying resident in VRAM at once; OmniScan now sends its own `num_ctx` and unloads every other local Ollama model before using one (`llm/ollama.py`, `pipeline/stages.py`) — translate stage 83.7s → 60.9s on a real chapter. Ran a real translation-model comparison (translategemma 12b/27b, gemma4 12b/31b, gemma4-31b-cloud, several judge combinations) against 5 real Pepper&Carrot chapters with official English text: the cloud model alone matches every judged ensemble and is 3-6x faster; the bigger local models don't fit in 16 GB VRAM and are both slower and no better (`docs/benchmarks/translation-model-comparison.md`). **Owner's decision: `gemma4:31b-cloud` alone is now the only enabled translation profile**, with `translategemma:12b` wired as its automatic fallback for when the cloud rate limit is hit (`TranslationProfile.fallback`, `resolve_fallbacks`, `TranslateStage`/`JudgeStage` rate-limit handling) — a chapter that falls back is retried against the cloud model on the next run. Also merged in the first half (by GLM builders, director-reviewed): **O1c** (OCR qualification suite), **O1d** (PaddleOCR-VL engine), **O1e** (fixed the series.toml-clobbers-candidate-config bug, core-side), **O1f** (batching investigation — reversed, see above), **C5c** (story memory + glossary term proposals from raw OCR text, no reference translation needed), **L1** (cover/metadata fetch: AniList/MangaDex/Jikan), **W1** (`model-watch` GitHub Action), **TL1** (every LLM prompt now names the chapter's real source language instead of hard-coding Korean — Chinese/Japanese series were being told their text was Korean; Korean prompts are byte-identical to before). **PaddleOCR-VL was recorded as `recommended_for` Korean and Chinese** in `config/models.toml` (a real tradeoff — best chrF but slower and, on Korean, a real recall loss — so it does not replace the existing ppocr recommendation; both are listed).

## What works today (CLI)
`doctor`, `hardware`, `models`, `update`, `import` (folders, `.zip`/`.cbz` archives, JPEG conversion), `ingest`, `slice`, `slice-compare`, `detect`, `ocr`, `filter run|restore`, `glossary` (`list`/`export`/`import`/`propose`), `reference` (D7 bootstrap), `match chapters` (CM1), `watermark`, `translate`, `judge`, `story summarize`, `inpaint [--lama]`, `typeset`, `export`, **`run`** (flags `-c`, `-s`, `--no-lama`, `--force`, `--step`, `--preview-chapter`), `eval`, `pack`, `serve` (+ `npm run dev` in `webui/` — now **six** chapter views: Slicer/OCR/Translation/Reader/Filtered plus **Inpaint** and **Layout**, B11b), `queue …`, **`gui`** (real PySide6 main window, **six pages**: Library/Reader/Run/Models/Settings/**Import** — the Import page is now wired into the sidebar). No stub commands remain. `acquire` no longer exists (removed, RM1).
Run everything with `uv run --frozen …` from `V:\OmniScan`. Data (gitignored): `data/raws/` (KoreanDemo, KoreanScan, PepperCarrot{,KR,CN,JA}, SoloLeveling — 2 bounded chapters imported this session as real commercial-manhwa test data), `data/translated-check/` (ground truth incl. SVG text layers), `data/output/`, `data/work/`+`data/work_qual/`; machine config `~/.config/omniscan/config.toml` (`library_root = data/raws`).

## Cross-process GPU exclusivity (still in force)
`omniscan.gpu.lock` (OS-level advisory lock) is wired into every real-GPU entry point: `cli.py`'s `run`/single-stage subcommands/`reference`, `queue/executor.py`, every `@pytest.mark.gpu` test. At most one process anywhere executes GPU code at a time. **Also new this session: at most one *local Ollama model* is resident in VRAM at a time** (`gpu/vram.py::VramManager.evict_ollama(keep=...)`, called by `pipeline/stages.py`'s translate/judge stage adapters before every profile) — two 12B local models kept loaded simultaneously measured 34s → 87s for one profile. Any new real-GPU entry point must acquire the GPU lock before `build_vram_manager` and release it in `finally` — copy the pattern from `cli.py`'s `_run_stages`.

## Translation defaults (changed this session — read before assuming the old 3-profile setup)
`config/translation_profiles.toml`: only `gemma4-31b-cloud` is `enabled` (was: translategemma-12b-local + gemma4-12b-local + gemma4-31b-cloud, judged). It carries `fallback = "translategemma-12b-local"`, which is `enabled = false` (only ever runs as a fallback). On an `OllamaRateLimitError` the affected chapter — and every later chapter in the same pass — translates with the fallback instead; the chapter's manifest record still names the primary's output file, so the *next* run retries it against the cloud model. The judge (`config/judge.toml`, still `gemma4:31b-cloud`) now only ever considers runs the current translate stage actually produced (`JudgeStage(run_ids=...)`), and survives its own rate limit the same way (unanswered regions keep the deterministic pick, flagged `judge_failed`, counted in the `rate_limited` metric). See `docs/benchmarks/translation-model-comparison.md` for the full evidence and `docs/tasks/TL1.md`'s report for the language-aware prompt work layered on top.

## Real commercial-manhwa test data (imported this session, bounded deliberately)
`data/raws/SoloLeveling/Chapter 1`, `Chapter 2` (Korean, from the owner's own downloader tool's already-downloaded `downloads/wfwf504` folder) and `_reference_en/Chapter 1`, `Chapter 2` (English, from `downloads/mgread`) — only 2 of 200+/204 available chapters, deliberately not the full commercially-licensed series. **Both raw chapters have now been run through the full pipeline and visually reviewed** (the four bugs above were all found this way); `omniscan match chapters` / `omniscan reference` against the `_reference_en` side has **not** been run yet — a reasonable next step if more glossary/quality signal is wanted, but the highest-value visual-review pass is done.

## Evidence gathered (all reproducible; details in `docs/benchmarks/` and `docs/reports/`)
- **Windows native works** (`windows-native.md`): AMD's `win_amd64` ROCm wheels run on the RX 9070 XT; GPU selection via `omniscan.gpu.device.resolve_device` (the iGPU is `cuda:0` and crashes, the 9070 XT is `cuda:1`).
- **fp16 is unreliable → everything runs fp32** (`windows-native.md` addendum, `docs/DECISIONS.md`).
- **G2/G3 (steady GPU load):** median wall 44.9s → 33.0s on PepperCarrotKR Episode 06 (merged prior session).
- **ocr-qualification-results.md (this session, post-fix):** real chapters 749-864s → 56-100s (8.6-9.1x) at identical chrF/recall, after the `use_cache=True` fix; PaddleOCR-VL now recommended for `ko`/`zh` by the qualification suite's own numbers (KO: 0.567 vs 0.475 chrF for the next-best; ZH: 0.741 vs 0.642) — **not yet promoted as the config default, see Open below**. `ppocr-v6-medium` promoted as the `ja` default (0.832 vs 0.778 chrF).
- **translation-model-comparison.md (this session):** cloud alone (0.239 chrF) matches every judged local+cloud combination (0.228-0.241) on 5 real Pepper&Carrot KR chapters; translategemma:27b and gemma4:31b-local both slower *and* not better than their 12B/cloud counterparts on this 16GB card (partial CPU offload). Hand-read spot check: translategemma (12b *and* 27b) reversed polarity on two lines ("Not innovative" for "innovative, isn't it") that both gemma4 models got right — chrF does not catch this class of error.
- **CM1 (chapter matching):** Needleman-Wunsch-style alignment, 100x200 chapters x 20 pages timed at 2.5-8.0s.
- **GL1 (reference-mode glossary):** real GPU e2e, CM1 matched 3/3 pairs, locked 2/3 terms at the D7 default. Known limitation: the reference side is OCR'd with the series' source-language engine (needs a per-side override, out of scope).
- **Builders on Windows:** `scripts/omni_builder.py` (3 concurrent slots, `PYTHONPATH` pinned to the worktree) runs `claude` against Ollama with `glm-5.3-flash:cloud`.

## Not done
Sound effects (**M10 — confirmed this session: zero `sfx`-kind regions on either real Solo Leveling chapter; the detector never fires that class on this content, e.g. a "bi ———" beep sound was read as ordinary bubble_text and literally transliterated, not translated as an SFX — this is a detector-model-level gap, not a threshold tweak, still needs the design decision the queue already flagged**), qualification-suite `model-watch` Action follow-through (W1 merged, catalog not yet auto-updated from a live run), debugger overlays and manual tools, packaging (U4/U5), public repo/releases decision (owner), the hybrid GPU JPEG codec (C2), Japanese source material (no download site yet), the glossary term matcher's particle stripping is Korean-only (flagged by TL1's builder, not yet scheduled), `omniscan match chapters`/`omniscan reference` against the Solo Leveling `_reference_en` side (not run — the visual-review pass took priority and found more value).

## Waiting on you (the user)
Full list: **[docs/OPEN_QUESTIONS.md](OPEN_QUESTIONS.md)**. Most pressing:
1. **Sound effects (M10).** Now confirmed on real data that the detector never emits an `sfx` region at all — needs a design decision (train/tune detection vs. a different approach) before any card can be written; blocked, not a quick fix.
2. **Promote PaddleOCR-VL as the sole Korean/Chinese OCR default** (today it's recorded as *a* recommendation alongside the existing one, not a replacement — a real quality-vs-speed-vs-recall tradeoff, see Evidence). Needs an explicit call if a full switch (not just cataloging) is wanted.
3. F2c (fixed-position watermark tool, unwired), the converter code the owner mentioned in `V:\Manhwa and Manga downloader` (still not pointed at a specific file/function), public repo / `OmniScan-releases` decision, E2/E3 fonts and lettering look, F9 (glyph rasterisation on CPU), GL1's per-side OCR override.

## How to work the loop (for the next session)
- **Builders:** `uv run python scripts/omni_builder.py run <ID>` from `V:\OmniScan` (default `glm-5.3-flash:cloud`). Launch as a **background tool call**. Worktrees go to `V:\OmniScan-wt\<ID>`. **`git worktree remove --force` can leave an empty directory behind**, which fools `omni_builder.py`'s `is_dir()` check into skipping `git worktree add` — always `rm -rf` the leftover directory before retrying (hit this session). A builder that ends at its turn limit has usually finished the work but not committed — check `git status` in its worktree first.
- **Card → review → merge:** write `docs/tasks/<ID>.md` (exact interfaces, numbered acceptance tests, file allowlist, stop-and-ask rule — the O1e/TL1 level of detail); after the builder finishes: `git rebase main` in the worktree, read `docs/reports/<ID>.md` in full (the Questions section catches real spec bugs), run ruff/pyright/pytest, **spot-check anything security- or measurement-critical yourself** (this session: re-verified TL1's rendered zh/ja prompts by hand rather than trusting the report's description), look at real output where it's visual, `git merge --ff-only`, push, remove the worktree and branch.
- **Core-owned edits held across a card:** when a card needs a `core/**` change the builder can't make (this session: O1e's second `series.toml` re-merge lived inside `core/stage.py::make_context`), the director makes that fix directly and flips the builder's "pin the bug" tests to their post-fix assertions in the same review pass — don't leave `main` red waiting for a second card.
- **No concurrent heavy tasks.** Never run multiple heavy processes (pytest suites, GPU pipelines, real-hardware benchmarks) at once — it starves everything (looks like a hang) and invalidates any timing data collected during the overlap. Established as a hard rule this session after CPU contention from redundant concurrent test runs made a builder look stuck for 2+ hours.
- **Legal/ethical boundaries reaffirmed this session:** declined to operate the owner's own manga-downloader tool directly (it only supports piracy-aggregator sites) even under repeated direct instruction; declined to import/process the full Solo Leveling series (200+/204 chapters, actively commercially licensed) — imported only 2 bounded chapters per side as test data instead. Both should stand without re-litigation absent a materially different request.
- **Data:** never commit raws or samples (`data/`, `library/`, `samples/` are gitignored); tests use synthetic fixtures.
- **The real value this session came from actually running the pipeline and looking at output, not from reading code.** All four bugs in the second half were found by re-running `omniscan run SoloLeveling` on genuinely commercial content and reading the rendered pages — none of them were visible in ~3 900 passing tests or in the simpler Pepper&Carrot fixtures. When "tune on real data" is the task, budget time for actually looking at images, not just running the suite.
- **A background test server can outlive its own `Stop-Process` call.** `Start-Process -FilePath "uv" -ArgumentList "run",...` spawns `uv` as a wrapper; killing `$p.Id` kills only the wrapper, not the child `python`/uvicorn process it launched, which keeps holding the port. Symptom: every request 404s with FastAPI's generic `{"detail": "Not Found"}` (not a route's own custom 404) even though the code is correct — check `Get-NetTCPConnection -LocalPort <port>` for the real owning PID before concluding a route is broken.
- **A `# type: ignore` comment only covers its own line.** Reformatting a single-line call with an ignore comment into a multi-line call moves the flagged argument off that line — pyright then reports it again on the argument's own new line. Move the comment to the exact line pyright flags, not just the call's opening line.
