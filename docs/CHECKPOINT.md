# Checkpoint — 2026-09-19, late evening (Windows native, `V:\OmniScan`)

**New session? Read `docs/HANDOFF.md` first** (reading order, working agreement with the owner, lessons), then this file, then `docs/NEXT.md` (the ordered work queue with builder cards).
`docs/PLAN.md` is the master plan (see its "Plan revisions" section); this file is the current-state snapshot. Repo: **`V:\OmniScan`**, remote `Nawid3333/OmniScan` on GitHub (`main` is pushed after every merge).
Local data lives in `V:\OmniScan\data\` (gitignored; see its `README.txt`): `raws/` (chapters to translate, = `paths.library_root`), `translated-check/` (ground truth with the same Series/Chapter layout: `english/`, `text-free/`), `output/` (finished pages), `work/` (pipeline artifacts, safe to delete); machine config in `C:\Users\limex\.config\omniscan\config.toml` (paths only; the GPU is chosen by `gpu.device = "auto"`).

## State in one paragraph
**The walking skeleton is finished.** `omniscan run SERIES` takes raw chapters to exported English slices through ten resumable stages in three passes (vision: ingest → slice → detect → OCR; text: translate with 3 models → judge; render: inpaint → LaMa → typeset → export), and the job queue runs the same machinery.
Everything is merged, reviewed and mutation-checked; `main` is clean and pushed; **no builder is running and no worktree is left**. About 2 650 tests, ruff and pyright clean, the full suite (including the GPU tests) passes in ~2–3 minutes.
**Quality safety net:** the end-to-end golden GPU test `tests/unit/test_e2e_synthetic.py` runs the real detector, OCR and LaMa models on three synthetic Korean pages with a fake LLM and compares the exported pages with the exact truth (matched regions 13/15, OCR CER 0.041, page integrity, English drawn in every bubble). It fails when the old codec race is put back, when pages are reordered, or when text colours are wrong.
**Real run (2026-09-19):** `omniscan run KoreanDemo` with the real Ollama models: up-to-date stages `skipped`, translate `done` (45.8 s, translategemma-12b + gemma4-12b + gemma4-31b-cloud), judge `done` (2.1 s; fixed translategemma's "Jena", kept "hyung", consistent names), typeset + export `done` (0.7 s) → correctly lettered English pages with the Korean removed, free text on gradients cleaned by LaMa.
**First real Korean run (2026-09-19, legal data):** Pepper&Carrot (CC BY 4.0, David Revoy) exists lettered in Korean (`kr_`), English (`en_`, the original) and text-free (`gfx-only`) for the same art. All 33 Korean episodes are downloaded with `scripts/fetch_pepper_carrot.py --lang kr --episodes 1-32,37` (284 pages, 462 MB in `data/`; `--lang cn|ja` works the same) into `data/raws/PepperCarrotKR/Episode NN/` (the Korean pages) and `data/translated-check/PepperCarrotKR/Episode NN/{english,text-free}/` (ground truth; all gitignored), **plus the text layer of every page**: `translated-check/PepperCarrotKR/Episode NN/truth/{kr,en}/EnnPpp.svg` (Inkscape SVGs with one `flowRoot` per text box: box + lines; 568 files). The lettered low-res pages are the SVG page scaled by `width/svg_width` and cropped 34 rows at the TOP (verified by image alignment); the chapter is already a vertical strip after ingest (the `E06.jpg` files are only thumbnails, not strips). `omniscan run PepperCarrotKR -c "Episode 06"`: 67 regions, detect 12 s, OCR 17 s, translate 187 s (3 profiles), judge 11 s, render 8 s → most bubbles and free text come out as correctly lettered English. Seen: a small `…근데…` bubble was missed by the detector, the calligraphic title and subtitle are left in Korean (display text policy = M10), leftover `..` dots after inpainting in one bubble, OCR errors on real Korean (CER clearly above the synthetic 0.04). The text-free `gfx` pages are not pixel-aligned with the lettered pages (e.g. 1694 vs 1660 px tall), so inpainting must be scored with an alignment step.
**Not done:** sound effects (the detector finds none), tuning on real commercial Korean webtoons (question A1 — the only real Korean pages are the Pepper&Carrot translations; commercial-style long-strip pages with real scan/compression artefacts are still missing), story memory, web views for the render stages, `acquire`, portability (the torch pin is ROCm-only, so only this PC can `uv sync`).

## What works today (CLI)
`doctor`, `import`, `ingest`, `slice`, `detect`, `ocr`, `filter run|restore`, `glossary list|export|import`, `watermark add|list|remove`, `translate` (candidate runs; needs `ocr.json`), `judge` (`final.json`), `inpaint [--lama]`, `typeset` (`layout.json`), `export` (`output/<Series>/<Chapter>/NNNN.jpg` + `export.json`),
**`run`** (all ten stages: `omniscan run KoreanDemo`, flags `-c`, `-s`, `--no-lama`, `--force`), `pack` (CBZ/PDF), `serve` (+ `npm run dev` in `webui/`), `queue add|list|run|pause|resume|cancel|retry|clear` (`queue add` defaults to `ingest` + `slice`; any of the ten stage names, run exactly as listed).
Stubs (exit 2): `acquire`, `reference`. Web views: Slicer, OCR (reads `ocr.json`), Translation, Reader, Filtered — none yet for inpaint/typeset.
Run everything with `uv run ...` from `V:\OmniScan`. The synthetic chapter is in `data/raws/KoreanDemo/` (`scripts/make_korean_chapter.py --series KoreanDemo`); its outputs are in `data/work/` and `data/output/`.

## Evidence gathered (all reproducible; details in `docs/benchmarks/` and `docs/reports/`)
- **Windows native works** (`windows-native.md`): AMD's `win_amd64` ROCm wheels run on the RX 9070 XT; GPU selection via `omniscan.gpu.device.resolve_device` (the iGPU is `cuda:0` and crashes, the 9070 XT is `cuda:1`; call `torch.cuda.set_device` before MIOpen work).
- **fp16 is unreliable → everything runs fp32** (`windows-native.md` addendum, `docs/DECISIONS.md`): RT-DETR fp16 works only at batch 8, the PP-OCR detector and LaMa fail in fp16 too. fp32 speeds: RT-DETR 31–65 tiles/s, PP-OCR det 25 ms per 960×704, LaMa 30 ms per 512² (steady state).
- **OCR** (`ocr-probe.md`): PP-OCRv5 server text-line detector + Korean recognition through `transformers`: CER 0.048 in the probe, 0.03–0.04 on synthetic pages; DB post-processing on the CPU with OpenCV (3.4 ms per page).
- **LaMa** (`lama-probe.md`): TorchScript `big-lama.pt` in fp32, 30 ms per 512² crop; every new input shape costs a 10–25 s warm-up → one fixed 512² window warmed once.
- **Detection on real art** (C3 report): on English hand-lettered Pepper&Carrot pages (out of domain) it finds titles and bubbles, misses some, puts low-confidence boxes on objects; on synthetic Korean pages it finds all regions (boxes ~15 px padded around the ink).
- **Codec:** rocJPEG cannot run here; CPU `turbo` ≈ 141–148 Mp/s (`codec.md`). **A CUDA-only race in `TurboCodec.decode_into` duplicated pages** (fixed `8c3a32f`, guarded by a GPU test and by the golden test).
- **Translation** (`translation-probe.md`): gemma4 needs `think:false`; cloud models ignore Ollama's JSON `format`; translategemma ignores the glossary unless locked terms are pre-substituted. Cloud models are reachable through the local daemon (`x:cloud` and `x:31b-cloud` names).
- **Mutation reviews** (`scripts/mutate.py`, lists in `tests/mutants/`): 61 mutants on run/queue/adapters, 129 on slicer/ingest, 115 on filter/glossary/importer — all real gaps closed by tests; the director's earlier reviews of C3–C7c (~150 mutants) likewise.
- **Builders on Windows:** `scripts/omni_builder.py` (3 concurrent slots, `PYTHONPATH` pinned to the worktree, hard limit 150 tool calls per run) runs `claude` against the local Ollama with `glm-5.3-flash:cloud`.

## Next, in priority order
The ordered queue with tiers is in **`docs/NEXT.md`**. In short: get the owner's **real Korean raws (A1)** and tune on them → live judge check on more data → story memory + glossary proposals (C5c) → web views for inpaint/typeset (B11') → sound effects (M10 design) → more mutation reviews and small fillers (B31/B32/B33) → portability (P1/P2) → hybrid codec decision.

## Waiting on you (the user)
The full list (about 50 questions with my defaults) is **[docs/OPEN_QUESTIONS.md](OPEN_QUESTIONS.md)** — ask 2–3 at natural pauses and record answers there. The most pressing:
- **A1: 1–2 real Korean raw chapters you own or may use** (`uv run omniscan import "<folder>" --series Sample --chapter "Chapter 1"`) — commercial-style long strips for the final tuning; Pepper&Carrot (Korean/English/text-free, CC BY 4.0) is already downloaded and covers real Korean lettering.
- **A4: the extract.pics API docs as pasted text** — the docs site is a JS app I cannot read; the API key is stored in `~/.config/omniscan/secrets.env` (`EXTRACTPICS_API_KEY`). Decision on record: unlicensed aggregator sites are not scraped (see `docs/DECISIONS.md`).
- **E2/E3:** which fonts and lettering look you want (official-release pages to match would help). **D5:** judge only on disagreements (built that way). **F9:** glyph rasterisation on the CPU.

## How to work the loop (for the next session)
- **Builders:** `uv run python scripts/omni_builder.py run <ID>` from `V:\OmniScan` (default `glm-5.3-flash:cloud`; `--model glm` only for genuinely hard cards or after flash failed; `resume <ID> <feedback-file> [--max-turns N]`; `smoke`). Launch it as a **background tool call** so the completion notification arrives; at most 3 at once
  (`OMNI_SLOTS=2` when a live Ollama check is planned). Worktrees go to `V:\OmniScan-wt\<ID>` with a junction to the shared `.venv`. A stuck builder can be stopped (kill the `omni_builder.py run <ID>` process tree with `taskkill /PID <uv pid> /T /F`), its session id copied from the log into `.builder/logs/<ID>.session`, and resumed with a feedback file.
  **Every run has a hard limit of 150 tool calls**; a run that ends at the limit has usually finished but not committed — look at `git status` in its worktree before anything else.
- **Card → review → merge:** write `docs/tasks/<ID>.md` (exact interfaces, numbered acceptance tests, file allowlist, stop-and-ask, "commit early"; compute golden values from real code first), `uv run ruff format` it and commit; after the builder finishes: in the worktree `git rebase main` (README/USER_GUIDE/stub-list conflicts are routine: keep both sides' additions, remove every implemented command from the stub lists), read `docs/reports/<ID>.md` (Deviations + Questions),
  run ruff/pyright/pytest **with `PYTHONPATH=<worktree>\src` and `.venv\Scripts\python.exe -m …`** (the shared editable install flips on every `uv run`), mutation-check the logic with `scripts/mutate.py` (a list of `(file, old, new, label)` in a scratch file; ~20–60 mutants; kill survivors with tests), look at real output where it is visual,
  add a "Review addendum (director)" to the report, `git merge --ff-only <ID>` in `V:\OmniScan`, run the suite on `main`, push, remove the worktree (delete the leftover `.venv` junction with `[System.IO.Directory]::Delete(path, $false)` — never recursive) and the branch.
- **Gotchas:** builders' `ruff format .` may reformat older un-formatted cards (`git checkout -- docs/tasks/<file>` before rebasing); a builder whose worktree was cut before a dependency merged may re-implement it (dedupe on review); the PowerShell tool resets its cwd on every call; use `uv run --frozen`; files with quotes are easier to write with the editor tools than with heredocs; symlinks need Developer Mode; `python -m omniscan` does not exist (use `python -c "from omniscan.cli import app; app()" <args>` with `PYTHONPATH` set); Korean output needs `PYTHONIOENCODING=utf-8`.
- **Data:** never commit raws or samples (`data/`, `library/`, `samples/` are gitignored); tests use synthetic fixtures (`tests/fixtures/korean_pages.py`).
