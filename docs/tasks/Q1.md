# Q1 — Mutation review of the slicer and the ingest layout

**Owner:** GLM builder · **Branch:** `Q1` · **Worktree:** `V:\OmniScan-wt\Q1` (created by `scripts/omni_builder.py`)
Read `CLAUDE.md` first. This card does **not** add features. It finds the tests that would still pass if the code were wrong ("surviving mutants") and adds tests that would catch them.
The modules below decide what every released chapter looks like, but their tests were only ever checked by reading them.

## Modules under review (production code — you must NOT change it)
- `src/omniscan/slicer/bands.py` (uniform-band detection)
- `src/omniscan/slicer/cuts.py` (cut planning, dynamic programming, forced cuts)
- `src/omniscan/slicer/slice.py`, `src/omniscan/slicer/page_mode.py`
- `src/omniscan/ingest/layout.py`, `src/omniscan/ingest/convert.py`
Their tests: `tests/unit/test_slicer_bands.py`, `test_slicer_cuts.py`, `test_slicer_slice.py`, `test_slicer_page_mode.py`, `test_slicer_properties.py`, `test_ingest_layout.py`, `test_ingest_convert.py`, `test_ingest.py`

## Protocol (follow it exactly)
1. Read each module and its tests. Create the untracked scratch directory `tmp_mut/` in the worktree (delete it at the end).
2. For **each mutant**: (a) copy the module to `tmp_mut/<name>.orig`; (b) make ONE small change with the Edit tool; (c) run only the module's test files: `uv run pytest tests/unit/test_slicer_bands.py tests/unit/test_slicer_cuts.py tests/unit/test_slicer_slice.py tests/unit/test_slicer_page_mode.py tests/unit/test_slicer_properties.py tests/unit/test_ingest_layout.py tests/unit/test_ingest_convert.py tests/unit/test_ingest.py -x -q -p no:warnings`;
   (d) record `KILLED` (a test failed) or `SURVIVED` (all passed); (e) restore the module with `cp tmp_mut/<name>.orig <original path>` and check `git diff --stat -- src` shows nothing.
   Never leave a mutation in place while doing anything else.
3. Aim for **at least 30 mutants per module file group** (about 70 in total), spread over these kinds: flip a comparison (`<` `<=` `>` `>=` `==` `!=`), off-by-one on a constant or index, `and` <-> `or`, remove a guard or an `if` branch,
   drop a `not`, swap `min`/`max`, change a constant (0/1, a threshold, a default), change a loop bound or slice end, swap two arguments, return the wrong variable, break tie-breaking/sorting, drop a `+ 1`/`- 1`, change rounding (`floor`/`ceil`/`round`).
   Prefer mutants that change behaviour a user would see. A mutant that cannot change any output (dead code, same result) is *equivalent*: do not count it, list it in the report.
4. For every **surviving non-equivalent** mutant write a test that **fails with the mutation applied and passes on the original**. Put it in the module's existing test file (append) or in `tests/unit/test_slicer_mutation_gaps.py`.
   Prove it: re-apply the mutant once, run the new test (it must fail), restore. Use small hand-made inputs with exact expected values, not random data.
5. At the end: `git diff --stat -- src` must be **empty** (no production change), `tmp_mut/` deleted, `uv run pytest -q` all green (the full run includes GPU tests), `uv run ruff format . && uv run ruff check .` clean, `uv run pyright` clean.
   If you believe production code has a real bug (a test you wrote against the intended behaviour fails on the original), do NOT fix it: write the finding under Questions with a minimal reproduction and mark the test `@pytest.mark.xfail(strict=True, reason="<what is wrong>")`.

## Files you may create / modify
- new tests: `tests/unit/test_slicer_mutation_gaps.py`; existing test files listed above (append only)
- `docs/reports/Q1.md` (create)
Nothing else. In particular no file under `src/`.

## Report (`docs/reports/Q1.md`)
Sections: **Summary** (mutants tried / killed at first / survived / equivalent / gaps closed), **Mutant table** (one row each: module:line, mutation, result before, test that kills it), **New tests**, **Equivalent mutants**, **Questions** (suspected bugs).
Commit `Q1: mutation review of the slicer and the ingest layout`. If anything is unclear: stop, write the question under Questions, commit what you have, and end.
