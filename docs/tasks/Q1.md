# Q1 — Mutation review of the slicer and the ingest layout

**Owner:** GLM builder · **Branch:** `Q1` · **Worktree:** `V:\OmniScan-wt\Q1` (created by `scripts/omni_builder.py`)
Read `CLAUDE.md` and `scripts/mutate.py` (the mutation harness, docstring only) first. This card does **not** add features. It finds the tests that would still pass if the code were wrong ("surviving mutants") and adds tests that would catch them.
The modules below decide what every released chapter looks like, but their tests were only ever checked by reading them.

## Modules under review (production code — you must NOT change it)
- `src/omniscan/slicer/bands.py` (uniform-band detection)
- `src/omniscan/slicer/cuts.py` (cut planning, dynamic programming, forced cuts)
- `src/omniscan/slicer/slice.py`, `src/omniscan/slicer/page_mode.py`
- `src/omniscan/ingest/layout.py`, `src/omniscan/ingest/convert.py`
Their tests: `tests/unit/test_slicer_bands.py`, `test_slicer_cuts.py`, `test_slicer_slice.py`, `test_slicer_page_mode.py`, `test_slicer_properties.py`, `test_ingest_layout.py`, `test_ingest_convert.py`, `test_ingest.py`

## Protocol (follow it exactly — a harness does the mechanical part; do NOT edit production files by hand)
1. Read each module and its tests (`Read` only; do not run anything yet). Create the untracked scratch directory `tmp_mut/` (delete it at the end).
2. **One mutants file per module group** (e.g. `tmp_mut/<group>.py`), written in a single `Write` call. It defines `MUTANTS = [(file, old, new, label), ...]` where `old` is a snippet that occurs **exactly once** in `file`
   (include neighbouring text if needed), `new` is the mutated snippet and `label` a few words (`"flip < to <="`). Write **25–35 mutants per group**, spread over these kinds: flip a comparison (`<` `<=` `>` `>=` `==` `!=`),
   off-by-one on a constant or index, `and` <-> `or`, remove a guard or an `if` branch, drop a `not`, swap `min`/`max`, change a constant (0/1, a threshold, a default), change a loop bound or slice end, swap two arguments,
   return the wrong variable, break tie-breaking/sorting, drop a `+ 1`/`- 1`, change rounding (`floor`/`ceil`/`round`). Prefer mutants that change behaviour a user would see; do not include mutants you already know are equivalent.
3. Validate, then run: `uv run python scripts/mutate.py check tmp_mut/<group>.py` (every `old` found exactly once, mutant compiles; fix invalid entries) and
   `uv run python scripts/mutate.py run tmp_mut/<group>.py -t <test file> -t <test file> ...` (the test files of that group, listed below). It applies each mutant, runs the tests, restores the file and prints one `KILLED` / `SURVIVED` line per mutant.
   A run of 30 mutants takes a minute or two: one tool call. The harness restores every file itself; afterwards `git diff --stat -- src` must show nothing.
4. For every `SURVIVED` mutant decide: *equivalent* (no output can change — list it in the report) or a real gap. For each real gap write a test (append to the module's existing test file, or in the gaps file named under "Files you may create")
   that **fails with the mutation and passes on the original**; small hand-made inputs, exact expected values, no random data. Then prove it with `uv run python scripts/mutate.py run tmp_mut/<group>.py -t <the test files> --only <indices of the former survivors>`:
   they must now print `KILLED`. Add a second round of ~10 fresh mutants around the code you just looked at if time allows.
5. **Commit early:** after each module group, append its rows to `docs/reports/Q1.md` and commit (`Q1: WIP <group>`); you have a hard limit of 150 tool calls in total, and a run that ends without a commit is thrown away. At about 110 calls stop mutating and write the final report.
6. At the end: `git diff --stat -- src` **empty** (no production change), `tmp_mut/` deleted, `uv run pytest -q` all green (the full run includes GPU tests), `uv run ruff format . && uv run ruff check .` clean, `uv run pyright` clean.
   If you believe production code has a real bug (a test you wrote against the intended behaviour fails on the original), do NOT fix it: write the finding under Questions with a minimal reproduction and mark the test `@pytest.mark.xfail(strict=True, reason="<what is wrong>")`.

## Files you may create / modify
- new tests: `tests/unit/test_slicer_mutation_gaps.py`; existing test files listed above (append only)
- `docs/reports/Q1.md` (create)
- `tmp_mut/` (untracked scratch, deleted at the end)
Nothing else. In particular no file under `src/`.

## Report (`docs/reports/Q1.md`)
Sections: **Summary** (mutants tried / killed at first / survived / equivalent / gaps closed), **Mutant table** (one row each: module:line, mutation, result before, test that kills it), **New tests**, **Equivalent mutants**, **Questions** (suspected bugs).
Commit `Q1: mutation review of the slicer and the ingest layout`. If anything is unclear: stop, write the question under Questions, commit what you have, and end.
