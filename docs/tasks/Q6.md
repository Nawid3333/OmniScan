# Q6 — Mutation review of the acquisition client/selection/completeness code and the slicer strategies

**Owner:** GLM builder · **Branch:** `Q6` · **Worktree:** `V:\OmniScan-wt\Q6` (created by `scripts/omni_builder.py`)
Read `CLAUDE.md` and `scripts/mutate.py` (the mutation harness, docstring only) first. This card does **not** add features. It finds the tests that would still pass if the code were wrong ("surviving mutants") and adds tests that would catch them.
The modules below were written by builders and reviewed by reading; their tests were never checked for strength.

## Modules under review (production code — you must NOT change it)
- Group `acquire`: `src/omniscan/acquire/extractpics.py` (retry/throttle/error mapping, polling, timeout), `pagerun.py` (page-run selection), `selection.py` (chapter list parsing and range grammar), `completeness.py` (findings and verdict), `plan.py` (states, credits, accepted.json), and — if merged when you start — `run.py` (the acquire loop: credit budget, stop-on-auth/quota, resume, completeness gate)
- Group `slicer`: `src/omniscan/slicer/strategies.py` (`fixed`, `simple_gutter`, gutter run finder, dispatcher, blank/source-file post-processing) and `slicer/compare.py`
Their tests: `tests/unit/test_acquire_extractpics.py`, `test_acquire_pagerun.py`, `test_acquire_selection.py`, `test_acquire_completeness.py`, `test_acquire_plan.py`, `test_acquire_run.py` (if present), `test_slicer_strategies.py`, `test_slicer_compare.py`, `test_slicer_compare_cli.py`. **Note:** `tests/mutants/acquire/` (card Q3) covers the older acquire modules (`download.py`, `drm.py`, `filters.py`, `sources.py`); do not repeat those.
Aim for about **35 mutants for `extractpics.py` + `pagerun.py`** (status-code branches, retry counts and back-off values, `Retry-After` cap, throttle window, timeout comparison, regex boundaries such as the thumbnail size 400 and `min_run`/`min_share`, tie-breaking), **30 for `selection.py` + `completeness.py` + `plan.py` (+ `run.py`)** (range grammar, number-vs-position basis, 5 % width tolerance, 50 %/200 % page-count thresholds, gap runs, verdict order, credit arithmetic) and **25 for `strategies.py` + `compare.py`** (cut arithmetic, merge rule, tie-break, gutter run length, median).

## Protocol (follow it exactly — a harness does the mechanical part; do NOT edit production files by hand)
1. Read each module and its tests (`Read` only; do not run anything yet). Create the directory `tests/mutants/acquire_slicer/` (the mutant lists are kept in the repo: see `tests/mutants/README.md`).
2. **One mutants file per module group** (e.g. `tests/mutants/acquire_slicer/<group>.py`), written in a single `Write` call. It defines `MUTANTS = [(file, old, new, label), ...]` where `old` is a snippet that occurs **exactly once** in `file`
   (include neighbouring text if needed), `new` is the mutated snippet and `label` a few words (`"flip < to <="`). Write **25–35 mutants per group**, spread over these kinds: flip a comparison (`<` `<=` `>` `>=` `==` `!=`),
   off-by-one on a constant or index, `and` <-> `or`, remove a guard or an `if` branch, drop a `not`, swap `min`/`max`, change a constant (0/1, a threshold, a default), change a loop bound or slice end, swap two arguments,
   return the wrong variable, break tie-breaking/sorting, drop a `+ 1`/`- 1`, change rounding (`floor`/`ceil`/`round`). Prefer mutants that change behaviour a user would see; do not include mutants you already know are equivalent.
3. Validate, then run: `uv run python scripts/mutate.py check tests/mutants/acquire_slicer/<group>.py` (every `old` found exactly once, mutant compiles; fix invalid entries) and
   `uv run python scripts/mutate.py run tests/mutants/acquire_slicer/<group>.py -t <test file> -t <test file> ...` (the test files of that group, listed below). It applies each mutant, runs the tests, restores the file and prints one `KILLED` / `SURVIVED` line per mutant.
   A run of 30 mutants takes a minute or two: one tool call. The harness restores every file itself; afterwards `git diff --stat -- src` must show nothing.
4. For every `SURVIVED` mutant decide: *equivalent* (no output can change — list it in the report) or a real gap. For each real gap write a test (append to the module's existing test file, or in the gaps file named under "Files you may create")
   that **fails with the mutation and passes on the original**; small hand-made inputs, exact expected values, no random data. Then prove it with `uv run python scripts/mutate.py run tests/mutants/acquire_slicer/<group>.py -t <the test files> --only <indices of the former survivors>`:
   they must now print `KILLED`. Add a second round of ~10 fresh mutants around the code you just looked at if time allows.
5. **Commit early:** after each module group, append its rows to `docs/reports/Q6.md` and commit (`Q6: WIP <group>`); you have a hard limit of 150 tool calls in total, and a run that ends without a commit is thrown away. At about 110 calls stop mutating and write the final report.
6. At the end: `git diff --stat -- src` **empty** (no production change), the mutants files committed under `tests/mutants/acquire_slicer/` (add a row for them to `tests/mutants/README.md`), `uv run pytest -q` all green (the full run includes GPU tests), `uv run ruff format . && uv run ruff check .` clean, `uv run pyright` clean.
   If you believe production code has a real bug (a test you wrote against the intended behaviour fails on the original), do NOT fix it: write the finding under Questions with a minimal reproduction and mark the test `@pytest.mark.xfail(strict=True, reason="<what is wrong>")`.

## Files you may create / modify
- new tests: `tests/unit/test_acquire_slicer_mutation_gaps.py`; existing test files listed above (append only)
- `docs/reports/Q6.md` (create)
- `tests/mutants/acquire_slicer/acquire.py`, `tests/mutants/acquire_slicer/slicer.py` (the mutant lists) and one added row in `tests/mutants/README.md`
Nothing else. In particular no file under `src/`.

## Report (`docs/reports/Q6.md`)
Sections: **Summary** (mutants tried / killed at first / survived / equivalent / gaps closed), **Mutant table** (one row each: module:line, mutation, result before, test that kills it), **New tests**, **Equivalent mutants**, **Questions** (suspected bugs).
Commit `Q6: mutation review of the acquisition code and the slicer strategies`. If anything is unclear: stop, write the question under Questions, commit what you have, and end.
